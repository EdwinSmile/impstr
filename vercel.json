{
  "version": 2,
  "buildCommand": "npm install --prefix server && npm run build --prefix server && npm run build --prefix client",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && npm install --prefix server && npm install --prefix client",
  "functions": {
    "api/**": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/socket.io(.*)",
      "destination": "http://localhost:4000/socket.io$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}